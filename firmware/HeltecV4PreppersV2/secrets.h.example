#pragma once
// Copie este arquivo para secrets.h (na mesma pasta) e troque os valores.
// secrets.h está no .gitignore: nunca vai para o GitHub.

// Chave AES-128 do mesh LoRa (16 bytes). TODAS as placas da rede precisam da mesma chave;
// quem tiver a chave consegue ler e enviar mensagens na rede.
static const uint8_t MESH_PSK[16] = {
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
    0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00
};

// Senha padrão do WiFi da placa (mínimo 8 caracteres). Pode ser trocada depois pelo app (aba CONF).
#define CFG_WPASS_DEF "troque-esta-senha"
